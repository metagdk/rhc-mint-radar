# Dependencies
node_modules/

# Environment & secrets (never commit)
.env
.env.*
!.env.example
*.pem
*.key
credentials*
secrets*

# Local dumps / debug (may contain session chain data)
state_tmp.json
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Editors / OS
.idea/
.vscode/
*.swp

# Netlify CLI local
.netlify/

# Optional local server build artifacts
dist/
